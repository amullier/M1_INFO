PROTO INT fact(k)
FUNC VOID main()
{
	INT n, i, t[2]
	PRINT "input n between 0 and 11:\n"
	READ n
	i :=0
	
	t[i] := fact(n)

	PRINT "f(",0,") = ", t[0], "\n"
	
}

FUNC INT fact(n)
{
	PRINT (n-1) , "\n"
	IF n
	THEN
		RETURN (n * fact(n-1))
	ELSE
		RETURN 1
	FI
}
